# 代码与文档综合观察报告

更新时间：2026-06-09

## 总体判断

猪猪家的厨房已经从开发态原型推进到可以自托管日常使用的版本。它不是菜谱展示站，也不应该在界面上表现得像一套冰冷的“系统”；它更适合被理解为家里两个人共同维护的厨房小本子：计划这周想吃什么，买菜时别漏东西，做饭时少翻来翻去，做完后把真实反馈留下。

当前主要风险已经明显下降：登录保护、签名 cookie、admin/member 权限、生产环境密钥和默认密码校验、数据模型扩展、购物清单落库、媒体上传持久化、CookLog 统计一致性、推荐、做饭模式、成就、PWA 和备份恢复都已落地。最新一轮把首页从静态周计划里拆出来，并补上做饭进度恢复和购物分类折叠，日常误导和移动端绕路风险继续下降。

最新收尾还清理了页面里的开发说明感：用户能看到的地方不再出现账号配置、环境变量、部署提示；品牌改为“猪猪家的厨房”，英文辅助名为 “Zhuzhu's Home Kitchen”。

## 已完成重点

1. 安全与登录
   - 签名 cookie 替代明文 token。
   - 业务页面和业务 API 默认要求登录。
   - admin/member 权限分层。
   - 同一设备可保持登录。
   - 固定显示名为猪猪和猪宝，旧账号 ID 保留兼容历史数据。

2. 主流程
   - 首页展示当前周真实状态，未规划时不展示旧周数据；页面主体已改用真实 API，不再依赖静态示例数据。
   - 周计划保存、复制、归档、重新生成接口已落地。
   - 购物清单从周计划生成，勾选、库存和手动项刷新不丢；移动端清单支持分类折叠、未买项优先和更大的触控面积。
   - 做饭模式完成后生成 CookLog，并直接进入记录编辑入口补照片、评分和复盘。
   - 做饭模式在同一设备保存当前步骤和随手备注，离开后再回来不丢进度。
   - CookLog 照片和菜谱封面上传持久化。

3. 推荐与回顾
   - 推荐服务统一考虑近期重复、评分、库存、耗时和 profile。
   - 推荐返回 reason。
   - 成就系统和 PWA 基础能力已落地。

4. 工程质量
   - `check:mojibake`、`lint`、`typecheck`、`test`、`test:e2e`、`build` 均可运行。
   - E2E 已改用独立测试库 `test-results/e2e.db` 和 3137 端口，不再污染日常 `dev.db`。
   - 备份、验证备份、恢复演练脚本已存在。
   - Docker 迁移包已支持全表 manifest、媒体引用校验和数据级 smoke。

## 当前判断

按第一版成品化目标，项目整体可用度为 100%。这不代表体验已经没有提升空间，而是核心闭环已经能支撑真实家庭使用。

2026-06-09 最新回归结果：`check:mojibake`、`lint`、`typecheck`、`test`、`test:e2e`、`build`、`backup`、指定最新备份的 `verify:backup`、`restore:drill`、`export:docker-data`、`docker:smoke` 数据级校验均已通过。完整 E2E 共 13 个场景全绿；Docker Compose 运行态 smoke 因本机没有 Docker CLI 尚未在本机执行，需要迁移到目标机器后再跑一次。

后续工作应优先放在：

- 真实手机上的计划、购物清单和做饭体验微调；购物清单已合并回计划页，移动端底栏保留打卡，后续重点转为真实手机环境下的密度复核。
- Docker 迁移路径已收敛到 `docker-data` 整包：`export:docker-data` 负责生成数据库、上传图、原图备份、server data 和 manifest，`docker:smoke` 负责校验全表计数和图片引用；Compose 运行态验收以目标机器实际 Docker CLI 输出为准。
- 文案继续去掉开发说明和术语感。
- 首页推荐理由、未规划状态、待记录状态的情绪价值继续打磨，但数据来源已回到真实当前周和真实购物清单。
- 上传失败、AI 失败、网络慢时的反馈统一。
- 周期性重跑备份恢复演练和质量门。

## NiniMenu 借鉴落地情况

已借鉴并落地：

- app/admin 权限分层。
- 图片上传备份、压缩和安全删除。
- 推荐服务 profile、近期去重、评分和库存加权。
- 购物清单持久化。
- 做饭模式计时器、步骤导航和本机进度恢复。
- 成就事件系统。
- PWA 基础。

仍可继续借鉴：

- 更细的购物分类 override。
- 更成熟的发布 smoke checklist。
- 更细腻的用户侧更新说明和维护说明拆分。
